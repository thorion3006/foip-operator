---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: failoverips.foip.noshoes.xyz
spec:
  group: foip.noshoes.xyz
  names:
    kind: FailoverIp
    listKind: FailoverIpList
    plural: failoverips
    shortNames:
    - foip
    - foips
    singular: failoverip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.targetNode
      name: Target
      type: string
    - jsonPath: .status.transitionID
      name: Transition
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: FailoverIp is the Schema for the failoverips API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FailoverIpSpec defines the desired state of FailoverIp.
            properties:
              failureThreshold:
                default: 3
                description: |-
                  FailureThreshold and RecoveryThreshold prevent transient health changes
                  from starting or immediately reversing a handoff.
                format: int32
                minimum: 1
                type: integer
              ip:
                description: IP is the failover IP address to manage, without a prefix
                  length.
                type: string
              minHealthySeconds:
                format: int32
                minimum: 0
                type: integer
              preferCurrentOwner:
                type: boolean
              probeComposition:
                description: ProbeComposition determines how a set of probe results
                  is aggregated.
                enum:
                - All
                - Any
                - Quorum
                type: string
              probeQuorum:
                format: int32
                minimum: 1
                type: integer
              probes:
                description: Probes is optional; an empty list enables node-health-only
                  operation.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                maxItems: 32
                type: array
              providerCooldownSeconds:
                default: 60
                description: ProviderCooldownSeconds is the minimum interval between
                  route mutations.
                format: int32
                minimum: 0
                type: integer
              recoveryPolicy:
                default: HoldDualOwnership
                description: |-
                  RecoveryPolicy defines the action after provider routing succeeds but
                  post-route traffic verification fails.
                enum:
                - HoldDualOwnership
                - RollbackProvider
                - CommitDegraded
                - ManualIntervention
                type: string
              recoveryThreshold:
                default: 2
                format: int32
                minimum: 1
                type: integer
              retryBaseSeconds:
                default: 2
                description: RetryBaseSeconds and RetryMaxSeconds bound transient
                  retry delays.
                format: int32
                minimum: 1
                type: integer
              retryMaxSeconds:
                default: 60
                format: int32
                minimum: 1
                type: integer
              secretName:
                description: SecretName names the Secret containing refreshToken and
                  userId.
                type: string
              stabilizationSeconds:
                default: 30
                format: int32
                minimum: 0
                type: integer
            required:
            - ip
            - secretName
            type: object
          status:
            description: FailoverIpStatus defines the observed state of FailoverIp.
            properties:
              candidateReason:
                type: string
              candidateSince:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAttemptedProviderMutationAt:
                format: date-time
                type: string
              lastConfirmedProviderMutationAt:
                format: date-time
                type: string
              lastError:
                type: string
              lastSuccessfulPhase:
                description: LastSuccessfulPhase is the last phase completed successfully.
                type: string
              lastTransitionAt:
                format: date-time
                type: string
              localOwners:
                description: LocalOwners is the set of nodes reporting local /32 ownership.
                items:
                  type: string
                type: array
              manualReconcileToken:
                type: string
              nextEligibleMutationAt:
                format: date-time
                type: string
              phase:
                description: Phase is the durable state-machine phase.
                enum:
                - Idle
                - Selecting
                - Stabilizing
                - PreparingTarget
                - TargetPrepared
                - RoutingProvider
                - VerifyingProvider
                - VerifyingTraffic
                - Committing
                - CleaningStaleOwners
                - Succeeded
                - Degraded
                - Blocked
                type: string
              phaseStartedAt:
                description: PhaseStartedAt records when the current phase began.
                format: date-time
                type: string
              providerObservedOwner:
                description: ProviderObservedOwner is the server ID observed from
                  the provider.
                type: string
              recoveryAction:
                description: |-
                  RecoveryPolicy defines the action after provider routing succeeds but
                  post-route traffic verification fails.
                type: string
              recoveryAttempts:
                format: int32
                type: integer
              retryCount:
                format: int32
                type: integer
              sourceNode:
                description: SourceNode is the committed owner at transition start.
                type: string
              targetNode:
                description: TargetNode is the selected destination for this transition.
                type: string
              transitionID:
                description: TransitionID identifies the logical handoff and fences
                  stale reconciles.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
