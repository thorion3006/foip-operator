---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: failoverips.foip.noshoes.xyz
spec:
  group: foip.noshoes.xyz
  names:
    kind: FailoverIp
    listKind: FailoverIpList
    plural: failoverips
    shortNames:
    - foip
    - foips
    singular: failoverip
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FailoverIp is the Schema for the failoverips API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              ip:
                description: ip is the failover IP address to manage (bare IP, no prefix length).
                type: string
              secretName:
                description: secretName names the Secret containing refreshToken and userId.
                type: string
            required:
            - ip
            - secretName
            type: object
          status:
            properties:
              assignedNode:
                description: assignedNode is the node confirmed as the provider-routed owner.
                type: string
              desiredNode:
                description: desiredNode is the target selected for the next or current ownership state.
                type: string
              preparedNode:
                description: preparedNode is the desired node that confirmed the /32 is present locally.
                type: string
              lastSyncAttempt:
                description: lastSyncAttempt is the RFC3339 timestamp of the last provider route attempt.
                type: string
              lastSyncSuccess:
                description: lastSyncSuccess is the RFC3339 timestamp of the last verified route handoff.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
