---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: failoverprobes.foip.noshoes.xyz
spec:
  group: foip.noshoes.xyz
  names:
    kind: FailoverProbe
    listKind: FailoverProbeList
    plural: failoverprobes
    shortNames:
    - fprobe
    - fprobes
    singular: failoverprobe
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FailoverProbeSpec defines a reusable, composable health check.
            properties:
              bodyMatch:
                type: string
              caBundleSecretRef:
                description: SecretKeySelector selects a key of a Secret.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              composition:
                description: ProbeComposition determines how a set of probe results
                  is aggregated.
                enum:
                - All
                - Any
                - Quorum
                type: string
              credentialHeader:
                type: string
              credentialSecretRef:
                description: SecretKeySelector selects a key of a Secret.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              expectedStatusMax:
                format: int32
                type: integer
              expectedStatusMin:
                format: int32
                type: integer
              failureThreshold:
                format: int32
                type: integer
              followRedirects:
                type: boolean
              headers:
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              initialDelaySeconds:
                format: int32
                type: integer
              insecureSkipVerify:
                type: boolean
              intervalSeconds:
                format: int32
                type: integer
              kubernetes:
                description: KubernetesReadinessTarget describes a Kubernetes object
                  readiness check.
                properties:
                  apiVersion:
                    type: string
                  expected:
                    type: string
                  jsonPath:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              method:
                type: string
              networkPolicy:
                description: ProbeNetworkPolicy controls which resolved destinations
                  a probe may reach.
                properties:
                  allowPrivateNetworks:
                    type: boolean
                  allowedCIDRs:
                    items:
                      type: string
                    type: array
                  deniedCIDRs:
                    items:
                      type: string
                    type: array
                type: object
              phase:
                description: ProbePhase controls when a probe participates in a transition.
                enum:
                - PreRoute
                - PostRoute
                - Continuous
                type: string
              quorum:
                description: Quorum is required when composition is Quorum.
                format: int32
                minimum: 1
                type: integer
              successThreshold:
                format: int32
                type: integer
              target:
                description: ProbeTarget describes the endpoint independently of any
                  edge product.
                properties:
                  address:
                    description: Address may contain ${targetNodeIP} or ${failoverIP},
                      or be an explicit DNS name.
                    type: string
                  host:
                    type: string
                  path:
                    type: string
                  port:
                    format: int32
                    type: integer
                  sni:
                    type: string
                type: object
              timeoutSeconds:
                format: int32
                type: integer
              type:
                description: ProbeType identifies a provider-neutral executor.
                enum:
                - TCP
                - TLS
                - HTTP
                - HTTPS
                - Kubernetes
                type: string
            required:
            - phase
            - type
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observations:
                items:
                  description: ProbeObservation contains only non-sensitive result
                    metadata.
                  properties:
                    consecutiveFailures:
                      format: int32
                      type: integer
                    consecutiveSuccesses:
                      format: int32
                      type: integer
                    name:
                      type: string
                    observedAt:
                      format: date-time
                      type: string
                    reason:
                      type: string
                    success:
                      type: boolean
                  required:
                  - name
                  - observedAt
                  - success
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
