---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: failoverips.foip.noshoes.xyz
spec:
  group: foip.noshoes.xyz
  names:
    kind: FailoverIp
    listKind: FailoverIpList
    plural: failoverips
    shortNames:
    - foip
    - foips
    singular: failoverip
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FailoverIp is the Schema for the failoverips API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              ip:
                description: ip is the failover IP address to manage (bare IP, no
                  prefix length).
                type: string
              secretName:
                description: |-
                  secretName is the name of the Secret containing netcup credentials.
                  The Secret must have keys loginName and password.
                type: string
            required:
            - ip
            - secretName
            type: object
          status:
            properties:
              assignedNode:
                description: assignedNode is the node the IP was last successfully
                  routed to via the netcup API.
                type: string
              desiredNode:
                description: desiredNode is the node the IP should be routed to.
                type: string
              lastSyncAttempt:
                description: lastSyncAttempt is the RFC3339 timestamp of the last
                  netcup API call attempt.
                type: string
              lastSyncSuccess:
                description: lastSyncSuccess is the RFC3339 timestamp of the last
                  successful netcup API call.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
